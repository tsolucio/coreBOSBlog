- mysql version 8 support in Reports (change column names to lowercase)
- code changes after first test on PHP 8
- PHP 7.4 support
  - eliminate 5.x code get_magic_quotes_runtime in phpMailer
  - change implode parameters in Report and Calendar
- coreBOS Standard Formatting: you would think this would have disappeared by now but it seems like this is a never ending task!!
  - format CSRF library and add it to CI
  - convert MSDOS EOL to UNIX EOL in vtlib
  - Inventory Modules, PBXManager, Reports, Calendar, Workflow, ListView: format, change quotes and eliminate warnings
- optimizations
  - sync inventory Edit View and standard modue edit templates: eliminate obsolete uitypes code
  - eliminate obsolete non-supported List View Colors reference in Global Search
  - delete obsolete code and initialize variable in Quick Create and Settings
  - delete unused variable in Reports and Inventory Modules
  - change this->db for adb throughout the application to reduce memory consumption
  - change variable for direct result usage in attachments
  - eliminate smoothscroll javascript library that is not used anymore
  - eliminate unnecessary IF, unused variable and move condition to avoid unnecessary check on error in workflow system
- library updates:
  - Smarty to 3.1.36
  - HTMLPurifier to 4.13.0


Developer



Decision Map. RAldine project.

feat(DecisionMap) add audit event for tracking execution
feat(DecisionMap) permit execution of Decision Maps without record ID context
feat(DecisionTable) code to save map, cleanup copied code, get field information
feat(DecisionTable) expression function selector
feat(DecisionTable) generator layout. ready to start functionality
feat(DecisionTable) support context variable substitution in preprocess expression
feat(DecisionTable) support IGNORE as a wildcard rule value
feat(Rule) add audit event for tracking execution
feat(Rule) permit webservice rule execution using map name besides ID
feat(Validations) support Decision Tables as valid expressions: __DoesNotPass__==false
fix(DecisionTable) evaluate preprocess with screen values
fix(DecisionTable) support evaluating expressions when we don't have a record context
fix(Workflow) merge evaluate rule data with context when context is empty and fill in record_id only when we have one


Functionality

Things
feat(Setting) remove checkbox and styling
feat(Settings) changing textBox class to lds
feat(Workflow) add help information to expression editor
fix(Account/Contacts) shipping address location map was loading main one.
- eliminate warning in Accounts custom validation
fix(QuickEdit) move open_contact_account_details to general JS so we can create contacts anywhere
fix(PageHeader) avoid error on load in pages with no page header
fix(Popup) present valdiation errors in popup quick create
fix(PriceBooks) revert currency formatting which is already done
fix(Reports) correct array structure for fields with no permission when trying to be accessed
fix(Reports) eliminate path from scheduled reports attachments
fix(Reports) set custom date ranges when editing report so they get saved
fix(Users) incorrect variable for user name in asterisk inline validation
fix(Zendesk) temporarily deactivate access
fix(cbQuestion) builder error when no question has been created
fix(ChangePassword) UX recommendations
fix(CSRF) show CSRF error message instead of generic error message
fix(Edit) submit on enter but not in textareas
fix(Email) add browser variables to email detailview, add missing head close and structure HTML
fix(Filter/QueryGenerator) set action to correctly get the default filter
- **IMPORTANT**: correct Net Total (bGD) calculation for individual tax grouping of financial fields
fix(WebService) apply module access restrictions in global search
- Tax modifications:
  - avoid error if taxname not found when change status event is triggered
  - avoid saving field and eliminate warning if tax name cannot be found
  - translate labels, format code and add TAXID to change status event
fix(LDS) Remove border on firefox search icon
feat(CRMEntity) Allow for users to be related through regular related lists
feat(Cron) Added timeout for 10 minutes to cronWatcherService if is running to restart
feat(Popup) make screen bigger
feat(Workflow) support web service IDs in MergeTemplateWith context variable so we can pass in users
fix(PurchaseOrder) do not fill billing and shipping address when converting from product
fix(QuickCreate) add fromlink for account field on contact form
fix(WebService:ChangePassword) add user exist check, support for non WSID and avoid die on error: unit tests
fix(Webservice) use CreateView permission for web serivce actions
fix(WS:Validate) check read permissions not update permissions
fix(WorkFlow) Avoid losing variable when explicitely set
fix(InventoryModules) cleanup and standardize HTML in inventory lines
fix(Invoice): update save_module function to accept custom fields images
fix(Leads) add map based validations to conversion step and apply LDS to error screen
fix(LoginSync) avoid login with no parameters
fix(MailConverter) correctly read special characters in attachment name
fix(MailManager/Converter) make sure encoding has standard 10 characters: eliminate '-i' for better special characters support
fix(ProductAutocomplete) Prevent submitting on enter when the autocomplete is open and enter is used to select an option
fix(ProductAutocomplete) Use converted price for MC, not actual
fix(QueryGenerator) we do not support uitype77 fields (yet)
fix(Workflow) change static reference to object reference to comply with method definition (and call only once)
fix(WorkFlow) Place back full template instead of fieldname when keepvariable is true
fix(Workflow) use DB variable to get order and sort by
i18n(App) pt_br
i18n(BusinessMap) pt_br
i18n(Validations) translate custom messages
i18nGlobal Variables: CSRF